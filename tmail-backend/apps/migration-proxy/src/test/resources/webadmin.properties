enabled=true
# 0 = bind a random free port, so the in-process test server never clashes with a
# locally running migration-proxy stack (which publishes webadmin on host 8000).
port=0
host=0.0.0.0
https.enabled=false
